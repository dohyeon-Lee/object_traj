# Shared between simulation/main_abs.py and deploy_franka/main.py
# data/035_power_drill_20200709_151335


# data_dir: data/bowl6      
# angle: 90              
# elevation: 0       
# scale: 1              
# eef_dir: SPH_lat165lon90z90
# center_offset: [0.10, 0.0, 0.05]  
# start_frame: 25 

data_dir: data/bowl6      
angle: 90              
elevation: 0       
scale: 0.4              
eef_dir: SPH_lat165lon90z90
center_offset: [0.10, 0.0, 0.05]  
start_frame: 25              # anchor traj start (frame 0) to center instead of mean

# data_dir: data/juice 
# angle: 90                   
# elevation: 0               
# scale: 1.0                   
# eef_dir: SPH_lat90lon-70z30              
# center_offset: [0.1, 0.0, 0.0]  

# Simulation-specific (simulation/main_abs.py)
steps: 1                     # MuJoCo steps per trajectory waypoint
control_freq: 50             # OSC control frequency (Hz)
show_eef: false               # overlay EEF trail and axes on rendered frames
initial: false               # anchor traj start (frame 0) to center instead of mean
dataset_cam_only: false        # only render dataset camera view (no robot visualization)
cam_distance: 2                   # dataset camera distance multiplier (>1 = farther, <1 = closer)
# Deploy-specific (deploy_franka/main.py)
tcp_offset: 0.145            # flange to gripper tip along flange Z (meters, Robotiq85)
